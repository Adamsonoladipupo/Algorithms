package Strings;

public class FirstNonRepeating {
    public static String firstNonReapeating(String word){
        for (int check = 0; check < word.length(); check++){
            
        }
    }
}
