package arrays;

public class LongestCommonPrefix {
    public static String longestCommonPrefix(String[]words){
        String commonPrefix = "";
        for(int loop = 0; loop < words.length; loop++){
            for(int checkWord = 0;checkWord< words[loop].length(); checkWord++){
                if
            }
        }
        return commonPrefix;
    }
}
